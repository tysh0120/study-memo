 #tech/python #lib/pandas
 df.loc[] だと行のインデクス名ー列名の指定
 df.iloc[] だと行番号ー列番号の指定

行は数値で、列は列名で指定したいとき

### 参照のみの時
普通に
```
df.loc["col1"].iloc[0]
```
のようにすれば参照できる

### 更新したいとき
上記のやり方だと、deprecatedのワーニングが出てしまう。
df.index[列名] でインデクスの順番が取得できるのでこれを利用。
```
df.loc[df.index['col1'], 0]
```
