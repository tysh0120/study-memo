#tech/python #lib/sklearn
scikit-learn, Keras, TensorFlowによる実践機械学習読書メモ
### グリッドサーチ
パラメタの組み合わせをしらみつぶしに実行して評価する

```python
from sklearn.model_selection import GridSearchCV
from sklearn.neighbors import KNeighborsClassifier

kn_clf = KNeibhborsClassifier(n_neighbors=5)
param_grid = [
	{'n_neighbors': [3, 5, 7],
	 'weight': ['distance', 'uniform']}	
]
grid_search =
	GridSearchCV(KNeighborsClassifier(),
			     param_grid={'n_neighbors': [3,5,7],
						     'weight': ['distance', 'uniform']},
			     cv=3)

```

### fit(X_train, y_train)
キーワード引数 cv に数字を指定して、クロスバリデーション分割数指定可能
### cv_results_
結果詳細

