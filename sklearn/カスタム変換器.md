#tech/python #lib/sklearn 
FunctionTransformerで変換器を作成する方法
[[BaseEstimator 1]]

numpy配列を受け取りnumpy配列を返す関数を渡す

```
from sklearn.preprocessing import FunctionTransformer

# 逆変換がある場合は、inverse_func キーワード引数に指定する
log_tarnsformer = FunctionTransformer(np.log, inverse_func=np.exp)

# 使用例
converted = log_transformer.transform(1,2,3)
# array([0., 0.69314718, 1.09861229])

# 逆変換の使用例
log_transformer.inverse_transform(converted)
# array([1.,2.,3.])
```