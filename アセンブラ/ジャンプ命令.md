### 常にジャンプする
jmp dest

### フラグレジスタの値に応じてジャンプ
jz dest     ; ゼロフラグのビットが1の時だけdestにジャンプ

jnz dest   ; ゼロフラグのビットが０の時だけdestにジャンプ


### 使い方
一致比較
cmp src1 src2
; src1 - src2 の結果でフラグレジスタをセット
; src1 - src2 == 0  -> ゼロフラグビット: 1

test src1 src2
; src1 && src2の結果によりフラグレジスタをセット
