#asm 

### 前提
nasmを導入
```bash
sudo apt-get install nasm
```

\XXX.s ファイル作成
```asm
global main   ; これがないとgccでエラーになる

main:
  push eax  0x1
  …  以下略
```

```bash
$ nasm -g -f elf32 XXX.s
# XXX.oが出力される
$ gcc -m32 XXX.o
# a.outが出力  (-oで出力ファイル名指定もＯＫ)
```
